# agentic-sql-data-analyst
An AI-powered data analytics application that converts natural language questions into PostgreSQL SQL queries using Google's Gemini LLM.  The application validates generated SQL, executes it safely on PostgreSQL, generates business insights, creates charts automatically, and exports analysis reports through an interactive Streamlit dashboard.
